<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>pAIrStudio — LSU</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="assets/mike_tiger.png">
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome for icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --lsu-purple: #461d7c;
            --lsu-gold: #fdd023;
            --lsu-light-purple: #5a2ca0;
            --grass-green: #a5d6a7;
            --dark-bg: #1a1a1a;
            --panel-bg: #2b2b2b;
            --light-panel: #f8f9fa;
            --text-light: #ffffff;
            --text-dark: #333333;
            --border-color: #444444;
            --success-color: #28a745;
            --shadow-dark: rgba(0, 0, 0, 0.3);
            --shadow-light: rgba(255, 255, 255, 0.1);
        }

        /* ===== GLOBAL STYLES ===== */
        * {
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(180deg, var(--lsu-purple) 0%, var(--lsu-light-purple) 20%, var(--grass-green) 80%);
            background-attachment: fixed;
            color: var(--text-light);
            overflow-x: hidden;
        }

        /* Subtle pattern overlay */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                linear-gradient(45deg, rgba(255,255,255,0.03) 25%, transparent 25%),
                linear-gradient(-45deg, rgba(255,255,255,0.03) 25%, transparent 25%);
            background-size: 60px 60px;
            pointer-events: none;
            z-index: 1;
        }

        /* ===== HEADER ===== */
        .lsu-header {
            background: linear-gradient(135deg, var(--lsu-purple), var(--lsu-light-purple));
            box-shadow: 0 4px 20px var(--shadow-dark);
            position: relative;
            z-index: 1000;
        }

        .navbar-brand {
            font-weight: 800;
            font-size: 1.5rem;
            color: var(--text-light) !important;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .logo-tiger {
            width: 50px;
            height: 50px;
            background: var(--lsu-gold);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 3px 10px var(--shadow-dark);
        }

        .version-badge {
            background: var(--lsu-gold);
            color: var(--lsu-purple);
            font-weight: 700;
            padding: 0.4rem 0.8rem;
            border-radius: 20px;
            font-size: 0.9rem;
            box-shadow: 0 2px 8px var(--shadow-dark);
        }

        /* ===== MAIN CONTENT ===== */
        .main-container {
            position: relative;
            z-index: 2;
            padding: 2rem 0;
        }

        .page-header {
            text-align: center;
            margin-bottom: 2rem;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .page-title {
          font-weight: 800;
          font-size: 3rem;
          color:#FFD700; 
          margin-bottom: 0.5rem;
          text-shadow: 0 3px 6px rgba(0, 0, 0, 0.4);
        }

        .page-subtitle {
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 300;
        }

        /* ===== PANELS ===== */
        .workspace-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            margin-top: 2rem;
        }

        .panel {
            background: var(--panel-bg);
            border-radius: 15px;
            border: 2px solid var(--border-color);
            box-shadow: 0 10px 30px var(--shadow-dark);
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .panel-header {
            background: linear-gradient(135deg, var(--lsu-purple), var(--lsu-light-purple));
            padding: 1rem 1.5rem;
            color: var(--text-light);
            font-weight: 700;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .panel-header .icon {
            font-size: 1.3rem;
            margin-right: 0.5rem;
        }

        .panel-body {
            padding: 1rem;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        /* ===== GAME PANEL ===== */
        .game-panel .panel-body {
            background: var(--dark-bg);
        }

        .game-canvas {
            width: 100%;
            height: 40vh;
            background: var(--dark-bg);
            border: 3px solid var(--border-color);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .canvas-placeholder {
            color: rgba(255, 255, 255, 0.5);
            font-size: 1.5rem;
            text-align: center;
        }

        /* ===== BLOCKLY PANEL ===== */
        .blockly-panel .panel-body {
            background: var(--light-panel);
            color: var(--text-dark);
        }

        .blockly-workspace {
            width: 100%;
            height: 55vh;
            border: 2px solid #ddd;
            border-radius: 10px;
            background: white;
            margin-bottom: 1rem;
        }

        /* ===== ACTION BUTTONS ===== */
        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 700;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
            color: white;
        }

        .run-btn {
            background: linear-gradient(135deg, var(--success-color), #20c997);
            box-shadow: 0 4px 15px rgba(40, 167, 69, 0.3);
        }

        .run-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(40, 167, 69, 0.4);
            color: white;
        }

        .reset-btn {
            background: linear-gradient(135deg, #cc5500, #a04400);
            box-shadow: 0 4px 15px rgba(204, 85, 0, 0.3);
        }

        .reset-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(204, 85, 0, 0.4);
            color: white;
        }

        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 992px) {
            .workspace-container {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
            
            .page-title {
                font-size: 2.5rem;
            }
            
            .game-canvas {
                height: 50vh;
            }
            
            .blockly-workspace {
                height: 45vh;
            }
        }

        @media (max-width: 576px) {
            .page-header {
                padding: 1rem;
            }
            
            .page-title {
                font-size: 2rem;
            }
            
            .action-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .action-btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
        }

        /* ===== ANIMATIONS ===== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .panel {
            animation: fadeInUp 0.6s ease-out;
        }

        .panel:nth-child(2) {
            animation-delay: 0.1s;
        }

        /* ===== SCROLLBAR STYLING ===== */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--lsu-gold);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #f4c430;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <header class="lsu-header">
        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid" style="max-width: 95%; padding: 0 2rem;">
                <a class="navbar-brand" href="#">
                    <div class="logo-tiger">🐅</div>
                    <span>pAIrStudio</span>
                </a>
                <div class="d-flex align-items-center gap-3">
                    <span class="version-badge">LSU v1.0</span>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <div class="main-container">
        <div class="container-fluid" style="max-width: 95%; padding: 0 2rem;">
            <!-- Page Header -->
            <div class="page-header">
                <h1 class="page-title">pAIrStudio</h1>
                <p class="page-subtitle">Warehouse Robot Simulation Platform</p>
            </div>

            <!-- Workspace -->
            <div class="workspace-container">
                <!-- Game Panel -->
                <div class="panel game-panel">
                    <div class="panel-header">
                        <span><i class="fas fa-gamepad icon"></i>Simulation Viewer</span>
                    </div>
                    <div class="panel-body">
                        <!-- Game Canvas -->
                        <div class="game-canvas" id="game-canvas">
                            <div class="canvas-placeholder">
                                <i class="fas fa-gamepad fa-3x mb-3"></i>
                                <div>Game will load here</div>
                                <small>Ready for Phaser!</small>
                            </div>
        </div>

        <!-- Welcome Screen -->
        <div id="welcome-screen">
            <div class="welcome-container">
                <h1>🤖 pAIr Studio</h1>
                <p>Welcome to the Warehouse Robot Programming Experiment</p>
                <p>You will program a robot to move boxes between conveyor belts using block-based programming. An AI assistant will help you along the way.</p>
                
                <div class="input-group">
                    <label for="participant-id-input">Participant ID (optional):</label>
                    <input type="text" id="participant-id-input" placeholder="Leave blank for auto-generated ID">
                </div>
                
                <button id="start-experiment-btn" class="btn btn-primary">Start Experiment</button>
            </div>
        </div>

        <!-- Game Screen -->
        <div id="game-screen">
            <div class="game-layout">
                <!-- Header -->
                <div class="header">
                    <h1>🤖 pAIr Studio</h1>
                    <div id="level-progress">Level 1 of 5</div>
                </div>

                <!-- Game Panel -->
                <div class="game-panel">
                    <div id="level-info">
                        <h2 id="level-title">Level Title</h2>
                        <p id="level-description">Level description goes here.</p>
                    </div>
                    
                    <div id="game-container"></div>
                    
                    <div id="hints-container"></div>
                    
                    <div id="status-message" class="status"></div>
                    
                    <div class="controls">
                        <button id="run-program-btn" class="btn btn-success">▶ Run Program</button>
                        <button id="reset-level-btn" class="btn btn-warning">🔄 Reset</button>
                    </div>
                    
                    <div class="controls" style="margin-top: 10px;">
                        <label for="speed-slider" style="flex: 0 0 auto; margin-right: 10px;">Speed:</label>
                        <input type="range" id="speed-slider" min="10" max="90" value="50" style="flex: 1;">
                    </div>
                </div>

                <!-- Programming Panel -->
                <div class="programming-panel">
                    <h3 class="panel-title">📦 Program Blocks</h3>
                    <div id="blockly-container"></div>
                </div>

                <!-- Chat Panel -->
                <div class="chat-panel">
                    <h3 class="panel-title">💬 AI Assistant</h3>
                    
                    <div id="pair-programmer-indicator">
                        <div id="pp-state-indicator">Mode</div>
                    </div>
                    
                    <div id="chat-messages">
                        <div class="chat-message assistant">
                            <div class="message-content">
                                Hi! I'm here to help you program the robot. Ask me anything about how to solve the levels!
                            </div>
                        </div>
                    </div>
                    
                    <form id="chat-form">
                        <input type="text" id="chat-input" placeholder="Type your message..." autocomplete="off">
                        <button type="submit" id="chat-submit">Send</button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Level Complete Modal -->
        <div id="level-complete-modal">
            <h2 id="level-status">Level Complete!</h2>
            <div class="modal-stats">
                <p id="level-time">Time: 0s</p>
                <p id="level-attempts">Attempts: 0</p>
            </div>
            <div class="modal-actions">
                <button id="next-level-btn" class="btn btn-primary">Next Level</button>
            </div>
        </div>

        <!-- Survey Screen -->
        <div id="survey-screen">
            <div id="survey-container"></div>
        </div>

        <!-- Complete Screen -->
        <div id="complete-screen">
            <div class="complete-container">
                <h1>🎉 Thank You!</h1>
                <p>You have completed the experiment.</p>
                <p>Your responses have been recorded.</p>
                <p>You may now close this window.</p>
            </div>
        </div>
    </div>

    <!-- Main Application Script (bundled) -->
    <script type="module" src="dist/bundle.js"></script>
</body>
</html>